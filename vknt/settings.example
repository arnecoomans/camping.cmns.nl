"""
Django settings for vknt project.

Generated by 'django-admin startproject' using Django 4.2.4.

For more information on this file, see
https://docs.djangoproject.com/en/4.2/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/4.2/ref/settings/
"""

from pathlib import Path
from django.utils.translation import gettext_lazy as _

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent


# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/4.2/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = '[INSERT YOUR SECRET KEY HERE]'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

ALLOWED_HOSTS = ['[INSERT YOUR HOST(S) HERE]']


# Application definition

INSTALLED_APPS = [
    ['django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',]
    'location', # Added for application
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'vknt.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': ['templates',],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
                'location.context_processors.setting_data', # Added for application
            ],
            'builtins': [
                'templatetags.markdown_extras', # Added for application
                'templatetags.replace', # Added for application
            ]
        },
    },
]

WSGI_APPLICATION = 'vknt.wsgi.application'


# Database
# https://docs.djangoproject.com/en/4.2/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}


# Password validation
# https://docs.djangoproject.com/en/4.2/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/4.2/topics/i18n/

LANGUAGE_CODE = 'nl-nl' # Change to your preferred language code

TIME_ZONE = 'UTC'

USE_I18N = True # Added for application
USE_L10N = True # Added for application
LANGUAGES = (
    ('en', _('English')),
    ('nl', _('Dutch')),
    #('fr', _('French')),
)
LOCALE_PATHS = [
    BASE_DIR / 'locale/', # Added for application
]
USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/4.2/howto/static-files/

STATIC_URL = 'static/'
STATIC_ROOT = BASE_DIR.joinpath('public').joinpath(STATIC_URL)
STATICFILES_DIRS = [
    BASE_DIR / "project_static",
]
MEDIA_URL = 'media/'
MEDIA_ROOT = BASE_DIR.joinpath('public').joinpath(MEDIA_URL)

# Default primary key field type
# https://docs.djangoproject.com/en/4.2/ref/settings/#default-auto-field

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

# Application configuration
LOGIN_REDIRECT_URL = '/'
LOGOUT_REDIRECT_URL = '/'

# Application configuration
# Change this configuration to your own liking. Default values are shown
APP_TITLE = 'Vakantieplanner'
META_DESCRIPTION = 'Vakantieplanner voor familie-reizen. Deel ervaringen en super locaties. Plan je vakantie met overnachting en camping. Plan een weekend weg in een b&b.'
MASTER_CSS = 'vknt.css'
PAGINATE = 12

ACTIVITY_SLUG = 'activity'
NEW_USER_DEFAULT_GROUP = 'communityuser'

GOOGLE_API_KEY = '[ADD YOUR GOOGLE API KEY FOR GEOLOCATION HERE]'
GOOGLE_MAPS_API_KEY = '[ADD YOUR GOOGLE MAPS API KEY HERE]'
NOMINATIM_USER_AGENT = 'vknt.cmns.nl v0.0.Dev'
DEPARTURE_CENTER = 'Domkerk, Achter de Dom 1, 3512 JN Utrecht, Netherlands'
MAX_LIST_DISTANCE_QUERIES = 10